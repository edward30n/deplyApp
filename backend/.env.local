# Configuración para desarrollo local
ENV=local

# Database (local PostgreSQL)
DATABASE_URI=postgresql://postgres:edward123@localhost:5432/recWay_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=edward123
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=recWay_db

# URLs
FRONTEND_URL=http://localhost:5173
BASE_URL=http://localhost:8000

# Server Configuration
HOST=0.0.0.0
PORT=8000
USE_PROXY_HEADERS=false

# File Watcher
ENABLE_FILE_WATCHER=true

# Storage (local files)
USE_AZURE_STORAGE=false
LOCAL_STORAGE_BASE=uploads

# Email Configuration (para desarrollo)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=edniduva@gmail.com
SMTP_PASSWORD=cmezynkoglzlmbrf
EMAIL_FROM=edniduva@gmail.com
EMAIL_FROM_NAME=SmartEpi

# Security Configuration (desarrollo - valores cómodos)
SECRET_KEY=recway-secret-key-change-in-production-very-important
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=43200  # 30 días para desarrollo
REFRESH_TOKEN_EXPIRE_DAYS=30

# API Configuration
PROJECT_NAME=RecWay
API_V1_STR=/api/v1
ENVIRONMENT=development

# CORS Configuration (automático para local, pero se puede especificar)
CORS_ORIGINS=["http://localhost:3000", "http://127.0.0.1:3000", "http://localhost:5173", "http://127.0.0.1:5173"]

# Logging (opcional)
LOG_LEVEL=DEBUG
